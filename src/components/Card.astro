---
const { title, text, url } = Astro.props 
---
<article class="w-60 h-60 rounded-lg sm:w-80 sm:h-80 hover:scale-110 hover:transition-all cursor-pointer hover:shadow-xl hover:shadow-black" >
    <a href={url} class="bg-slate-200 block w-full h-full rounded-lg" >
        <figure class="w-full h-1/2 object-cover" >
            <slot />
        </figure> 
        <h3 class="text-lg sm:text-2xl font-bold text-center" >{title}</h3>
        <p class="text-[12.5px] sm:text-base text-center text-slate-700 px-2" >{text}</p>
    </a>
</article>