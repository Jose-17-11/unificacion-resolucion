---
const { title, text } = Astro.props 
---
<article class="w-60 h-60 sm:w-80 sm:h-80 hover:scale-110 hover:transition-all cursor-pointer" >
    <a href="#" class="bg-slate-400 block w-full h-full rounded-lg " >
        <figure class="w-full h-1/2 object-cover" >
            <slot />
        </figure> 
        <h3 class="text-2xl font-bold text-center" >{title}</h3>
        <p class="text-center text-slate-700" >{text}</p>
    </a>
</article>